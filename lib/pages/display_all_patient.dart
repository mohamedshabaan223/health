import 'package:flutter/material.dart';

class DisplayAllPatient extends StatelessWidget {
  const DisplayAllPatient({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Display All Patient'),
      ),
    );
  }
}
